<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tianji.promotion.mapper.UserCouponMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.tianji.promotion.domain.po.UserCoupon">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="coupon_id" property="couponId" />
        <result column="term_begin_time" property="termBeginTime" />
        <result column="term_end_time" property="termEndTime" />
        <result column="used_time" property="usedTime" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <select id="getAvailableCoupons" resultType="com.tianji.promotion.domain.po.Coupon">
        select c.id,
               c.discount_type,
               c.specific,
               c.threshold_amount,
               c.discount_value,
               c.max_discount_amount,
               uc.id as `creater`
        from user_coupon uc
                 inner join coupon c on uc.coupon_id = c.id and uc.status = 1
    </select>

</mapper>
